My dotfiles.

## Install

```bash
./install
```

Requirements: Git, Python (for Dotbot), and Homebrew-installed dependencies from `boostrap.sh` if you want the full setup.

Dotbot links the configs defined in `install.conf.yaml` (Zsh, eza/fastfetch/starship/ghostty/zed, git config/ignore, Raycast scripts).
