add_newline = true

# Vercel-style layout: directory → git → prompt
format = """\
$directory$git_branch$git_status
$character\
"""

continuation_prompt = "… "
[character]
success_symbol = "[ 󰘍](bold fg:#ededed) "
error_symbol = "[](bold fg:#e46d69) "

# ─────────── Hostname (clean / subtle) ───────────
[hostname]
ssh_only = true
format = "[$hostname](fg:#a1a1a1) "
disabled = false

# ─────────── Directory (Vercel-black + sharp edges) ───────────
[directory]
style = "bold fg:#ededed"
format = "[$path]($style) "
truncation_length = 3
truncate_to_repo = true
read_only = " "
read_only_style = "fg:#676767"

# ─────────── Git Branch (clean, gray, subtle) ───────────
[git_branch]
symbol = " "
format = "[$symbol$branch](fg:#a1a1a1) "
truncation_length = 15
truncation_symbol = ""

# ─────────── Git Status (simple, monochrome) ───────────
[git_status]
style = "fg:#676767"
format = "([$all_status$ahead_behind]($style)) "

# ─────────── Git State (rebase/merge indicators) ───────────
[git_state]
format = "([$state( $progress_current of $progress_total)])(fg:#a1a1a1) "

# ─────────── Command Duration (subtle right-aligned utility) ───────────
[cmd_duration]
min_time = 200
format = "[⏱ $duration](fg:#676767) "

# ─────────—— Language Runtimes (show only when needed) ─────────——
[nodejs]
format = "node $version "
style = "fg:#a1a1a1"

[python]
format = "py $version "
style = "fg:#a1a1a1"

[rust]
format = "rust $version "
style = "fg:#a1a1a1"

[golang]
format = "go $version "
style = "fg:#a1a1a1"

# All other languages are disabled for Vercel minimalism.
# Enable only what you actually use.

[docker_context]
symbol = " "
format = "docker "
style = "fg:#a1a1a1"

[kubernetes]
disabled = true

# ─────────── OS/Status/etc (disabled by default for minimal look) ───────────
[aws]
disabled = true
[gcloud]
disabled = true
[azure]
disabled = true
[battery]
disabled = true
[memory_usage]
disabled = true
[package]
disabled = true
[shell]
disabled = true
[status]
disabled = false
format = "[✘ $status](fg:#e46d69) "

[jobs]
disabled = false
style = "fg:#676767"
symbol = "• "

[container]
disabled = true
[sudo]
disabled = true
